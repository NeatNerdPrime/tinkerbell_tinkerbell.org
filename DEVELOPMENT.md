# Development Notes

- Update the Hugo version by updating `netlify.toml`
- Update the `geekdoc` theme by deleting everything in `themes/hugo-geekdoc/` and then [downloading](https://github.com/thegeeklab/hugo-geekdoc/releases) the tar.gz and extracting it into `themes/hugo-geekdoc`.
